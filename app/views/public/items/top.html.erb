<% if admin_signed_in? %>
  
  <h1>管理者画面</h1>

  本日の注文件数 : <%= Order.where("DATE(created_at) = '#{Date.today}'")%> 件

<% else %>
  <h1>商品一覧</h1>

  <% @items.each do |item| %>
    <%= link_to item.name, item_path(item.id) %>
  <% end %>

<% end %>